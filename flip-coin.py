import random

#flip = random.choice(["Heads","Tails"]) #make choices between in the list elements with same prob.

#print(flip)

#generate = random.randint(1,100) # creates random variable between the specified range.

#print(generate)


cards = ["jack","quenn","king","ace"]

random.shuffle(cards)

print(cards)

